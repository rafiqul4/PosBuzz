{
  "name": "posbuzz-api",
  "version": "1.0.0",
  "description": "PosBuzz API for Vercel Serverless Deployment",
  "scripts": {
    "build": "prisma generate --schema=./backend/prisma/schema.prisma",
    "vercel-build": "prisma generate --schema=./backend/prisma/schema.prisma",
    "postinstall": "prisma generate --schema=./backend/prisma/schema.prisma || true"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^22.10.7",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.7.3"
  }
}
